export default function LoginPage() {
  return (
    <section className="py-8">
      <h1 className="text-xl font-display font-semibold">Login</h1>
      <p className="mt-2 text-sm text-secondary-text">Sign-in page placeholder (auth will be added later).</p>
    </section>
  );
}
